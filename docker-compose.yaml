# Test GCS emulator

version: "3.0"
services:
  server:
    image: jwhitlock/gc-fake-storage:test
    volumes:
      - ./server/init_buckets:/data

  client:
    build: ./client
    depends_on:
      - server
    environment:
      - SERVER_URL=https://server:4443

